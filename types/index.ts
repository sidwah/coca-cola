export interface Product {
  id: string;
  name: string;
  description: string;
  image: string;
  color: string;
}

export interface NavigationLink {
  name: string;
  href: string;
}

// Add other types